#include <stdio.h>

int main() {
    int n, i;
    printf("Enter the number of students: ");
    scanf("%d", &n);

    char name[n][50]; // Array to store student names
    int marks[n];     // Array to store marks

    // Input names and marks
    for(i = 0; i < n; i++) {
        printf("Enter name of student %d: ", i + 1);
        scanf("%s", name[i]);
        printf("Enter marks of %s: ", name[i]);
        scanf("%d", &marks[i]);
    }

    // Find topper
    int maxIndex = 0;
    for(i = 1; i < n; i++) {
        if(marks[i] > marks[maxIndex]) {
            maxIndex = i;
        }
    }

    printf("Topper is %s with %d marks.\n", name[maxIndex], marks[maxIndex]);

    return 0;
}
O/P:
Enter the number of students: 3
Enter name of student 1: Alice
Enter marks of Alice: 85
Enter name of student 2: Bob
Enter marks of Bob: 92
Enter name of student 3: Charlie
Enter marks of Charlie: 78
Topper is Bob with 92 marks.
